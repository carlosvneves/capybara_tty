---
phase: 01-core-chess-engine-ui
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/chess_core.rs
  - src/engine.rs
  - src/lib.rs
  - tests/phase1_core.rs
autonomous: true
requirements: [REQ-001, REQ-007]
requirements_addressed: [REQ-001, REQ-007]
must_haves:
  truths:
    - PGN input applies only legal SAN moves
    - Ambiguous or malformed SAN never mutates position
    - Embedded engine depth mapping is fixed at 1/3/5
  artifacts:
    - path: src/chess_core.rs
      provides: SAN parse/apply contract backed by shakmaty
    - path: src/engine.rs
      provides: deterministic depth-based engine move selection
    - path: src/lib.rs
      provides: wasm-bindgen exports consumed by JS shell
  key_links:
    - from: src/lib.rs
      to: src/chess_core.rs
      via: exported apply-move wrappers
    - from: src/lib.rs
      to: src/engine.rs
      via: exported engine-move wrappers
---

<objective>
Build Rust/WASM core contracts for legal PGN move handling and deterministic embedded engine depth mapping.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Red tests for SAN handling and fixed depth mapping</name>
  <files>
    tests/phase1_core.rs
    tests/web.rs
  </files>
  <read_first>
    .planning/phases/01-core-chess-engine-ui/01-CONTEXT.md
    src/lib.rs
    tests/web.rs
  </read_first>
  <action>
    Add failing tests in tests/phase1_core.rs for: valid SAN accepted, malformed SAN rejected, ambiguous SAN rejected, depth mapping Easy=1 Medium=3 Hard=5.
  </action>
  <verify>
    <automated>cargo test --test phase1_core</automated>
  </verify>
  <acceptance_criteria>
    Running cargo test --test phase1_core fails first with missing functions and named tests assert valid_san_applies_move, malformed_san_rejected, ambiguous_san_rejected, depth_map_easy_1_medium_3_hard_5.
  </acceptance_criteria>
  <done>
    tests/phase1_core.rs and tests/web.rs include failing RED tests for SAN and depth mapping.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Implement chess core with shakmaty and wasm exports</name>
  <files>
    src/chess_core.rs
    src/engine.rs
    src/lib.rs
  </files>
  <read_first>
    Cargo.toml
    src/lib.rs
    .planning/phases/01-core-chess-engine-ui/01-RESEARCH.md
  </read_first>
  <action>
    Create src/chess_core.rs and src/engine.rs; parse/apply SAN using shakmaty; reject malformed/ambiguous SAN with explicit result kinds; wire #[wasm_bindgen] exports in src/lib.rs; keep deterministic engine depth selection 1/3/5.
  </action>
  <verify>
    <automated>cargo test --test phase1_core && wasm-pack build --target web</automated>
  </verify>
  <acceptance_criteria>
    cargo test --test phase1_core passes and wasm-pack build --target web exits 0; applying malformed or ambiguous SAN returns explicit invalid result without position change.
  </acceptance_criteria>
  <done>
    Core modules compiled and exported; automated verify command exits 0.
  </done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Malformed PGN input | strict SAN parse before state mutation |
| Unbounded search | clamp depth to 1/3/5 only |
</threat_model>

<verification>
cargo test && wasm-pack build --target web
</verification>
