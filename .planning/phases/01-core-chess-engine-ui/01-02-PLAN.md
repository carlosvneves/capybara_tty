---
phase: 01-core-chess-engine-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - www/index.html
  - www/styles.css
  - www/main.js
autonomous: true
requirements: [REQ-003, REQ-004]
requirements_addressed: [REQ-003, REQ-004]
must_haves:
  truths:
    - Landscape mode shows two-column board/panel shell
    - Portrait mode shows rotate message
    - Unicode pieces default; ASCII toggle works globally
  artifacts:
    - path: www/index.html
      provides: shell structure for board and panels
    - path: www/styles.css
      provides: landscape/portrait layout and board sizing tokens
    - path: www/main.js
      provides: renderer and piece-mode toggle behavior
  key_links:
    - from: www/main.js
      to: www/styles.css
      via: --board-font-size CSS variable usage
---

<objective>
Build TUI-style landscape shell and board renderer with Unicode default plus ASCII fallback.
</objective>

<tasks>
<task type="auto">
  <name>Create landscape shell with rotate fallback</name>
  <files>
    www/index.html
    www/styles.css
  </files>
  <read_first>
    .planning/phases/01-core-chess-engine-ui/01-UI-SPEC.md
    .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Implement two-column layout in www/index.html and www/styles.css (board ~55%, panels ~45%), command input pinned visible, and portrait rotate message.
  </action>
  <verify>
    <automated>wasm-pack build --target web && wasm-pack test --headless --chrome</automated>
  </verify>
  <acceptance_criteria>
    When viewport is landscape, board column and panel column render side-by-side; when viewport is portrait, rotate message is visible and game shell is hidden.
  </acceptance_criteria>
  <done>
    Landscape and portrait behaviors match acceptance criteria in browser runtime.
  </done>
</task>

<task type="auto">
  <name>Implement piece mode toggle and board font token</name>
  <files>
    www/main.js
    www/styles.css
  </files>
  <read_first>
    .planning/phases/01-core-chess-engine-ui/01-UI-SPEC.md
    www/main.js
  </read_first>
  <action>
    Add board renderer in www/main.js with Unicode default pieces and ASCII fallback toggle; drive piece size via CSS variable --board-font-size.
  </action>
  <verify>
    <automated>node --check www/main.js && wasm-pack build --target web && wasm-pack test --headless --chrome</automated>
  </verify>
  <acceptance_criteria>
    Default render shows Unicode chess symbols for all pieces; switching setting to ASCII re-renders all pieces as KQRBNP/kqrbnp without page reload.
  </acceptance_criteria>
  <done>
    Piece mode toggle works end-to-end with unicode default and ascii fallback.
  </done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Invalid renderer mode values | enum-validate mode to unicode/ascii |
| Input flood causing render churn | state-driven rerender only |
</threat_model>

<verification>
wasm-pack build --target web
</verification>
