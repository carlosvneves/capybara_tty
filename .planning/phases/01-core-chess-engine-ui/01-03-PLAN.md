---
phase: 01-core-chess-engine-ui
plan: 03
type: execute
wave: 2
depends_on: [01-01, 01-02]
files_modified:
  - www/main.js
  - www/styles.css
  - src/lib.rs
autonomous: true
requirements: [REQ-007]
requirements_addressed: [REQ-007]
must_haves:
  truths:
    - Pre-game menu is fully keyboard-driven
    - Time-control presets include UltraBullet, Bullet, Blitz, Rapid, Classical, No clock, Custom
    - Embedded difficulty mapping is exactly Easy=1 Medium=3 Hard=5
  artifacts:
    - path: www/main.js
      provides: config menu state machine and startup payload creation
    - path: src/lib.rs
      provides: startup payload contract into wasm
  key_links:
    - from: www/main.js
      to: src/lib.rs
      via: start game payload including engine and depth
---

<objective>
Implement keyboard-only pre-game configuration menu and startup contract.
</objective>

<tasks>
<task type="auto">
  <name>Add config menu keyboard state machine</name>
  <files>
    www/main.js
    www/styles.css
  </files>
  <read_first>
    .planning/phases/01-core-chess-engine-ui/01-UI-SPEC.md
    .planning/phases/01-core-chess-engine-ui/01-CONTEXT.md
  </read_first>
  <action>
    Implement menu options in order: Engine, Time Control, Color, Bot Difficulty, Tutor Mode, Physical Board Mode. Engine values must be exactly Embedded and Stockfish. Startup payload must include selected engine value; Embedded uses depth mapping now, Stockfish is flagged as deferred runtime path for Phase 2. Support arrows to navigate and Enter to confirm. Time Control presets must be exactly UltraBullet, Bullet, Blitz, Rapid, Classical, No clock, Custom. Custom accepts minutes-only input.
  </action>
  <verify>
    <automated>wasm-pack build --target web && wasm-pack test --headless --chrome</automated>
  </verify>
  <acceptance_criteria>
    Pressing left/right on Engine cycles Embedded and Stockfish; Enter starts game with selected engine value in payload. Pressing left/right on Time Control cycles UltraBullet, Bullet, Blitz, Rapid, Classical, No clock, Custom in that order.
  </acceptance_criteria>
  <done>
    Keyboard navigation and exact time-control preset order implemented and runnable.
  </done>
</task>

<task type="auto">
  <name>Bind embedded difficulty to fixed depths</name>
  <files>
    www/main.js
    src/lib.rs
  </files>
  <read_first>
    src/lib.rs
    .planning/phases/01-core-chess-engine-ui/01-CONTEXT.md
  </read_first>
  <action>
    Map Easy->1, Medium->3, Hard->5 for embedded engine only and pass selected depth in start-game payload to wasm layer.
  </action>
  <verify>
    <automated>cargo test && wasm-pack build --target web && wasm-pack test --headless --chrome</automated>
  </verify>
  <acceptance_criteria>
    www/main.js contains explicit mapping object Easy:1 Medium:3 Hard:5 and passes selected depth only when engine mode is Embedded.
  </acceptance_criteria>
  <done>
    Embedded engine startup payload always carries depth 1/3/5 from selected difficulty.
  </done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Invalid menu value injection | clamp to known options only |
| Bad custom minutes input | numeric parse + bounds check |
</threat_model>

<verification>
cargo test && wasm-pack build --target web
</verification>
