---
phase: 01-core-chess-engine-ui
plan: 04
type: execute
wave: 3
depends_on: [01-01, 01-03]
files_modified:
  - www/main.js
  - www/styles.css
  - src/lib.rs
  - tests/web.rs
autonomous: true
requirements: [REQ-001, REQ-009, REQ-003]
requirements_addressed: [REQ-001, REQ-009, REQ-003]
must_haves:
  truths:
    - PGN command input drives move submission
    - Physical mode pauses after engine move and resumes on Space
    - No clock hides clock UI and timeout triggers immediate forfeit
    - Game-over banner is inline and board stays visible until restart
  artifacts:
    - path: www/main.js
      provides: runtime game loop and command handling
    - path: src/lib.rs
      provides: move/apply/export integration points
    - path: tests/web.rs
      provides: wasm behavior checks for runtime transitions
  key_links:
    - from: www/main.js
      to: src/lib.rs
      via: submit-move and engine-move wasm calls
---

<objective>
Complete game loop integration: command input, physical board sync, clocks, game-over and restart flows.
</objective>

<tasks>
<task type="auto">
  <name>Wire command input to wasm and lock invalid handling</name>
  <files>
    www/main.js
    src/lib.rs
  </files>
  <read_first>
    .planning/phases/01-core-chess-engine-ui/01-CONTEXT.md
    src/lib.rs
    www/main.js
  </read_first>
  <action>
    On Enter, submit PGN to wasm apply API. Valid move updates board/history. Malformed or ambiguous SAN triggers red flash + clear only. Do not keep persistent invalid message.
  </action>
  <verify>
    <automated>wasm-pack build --target web && wasm-pack test --headless --chrome</automated>
  </verify>
  <acceptance_criteria>
    Submitting malformed SAN leaves board unchanged, clears input, and removes error style on next keystroke; no persistent error text remains.
  </acceptance_criteria>
  <done>
    Command input behavior matches valid/invalid flow and no persistent error message remains.
  </done>
</task>

<task type="auto">
  <name>Implement physical board pause/resume and clock rules</name>
  <files>
    www/main.js
    www/styles.css
  </files>
  <read_first>
    .planning/REQUIREMENTS.md
    .planning/phases/01-core-chess-engine-ui/01-CONTEXT.md
  </read_first>
  <action>
    After engine move in physical mode, set waiting state and resume only on Space key. Hide clock when No clock selected. Timeout causes immediate forfeit. Engine move never consumes clock time.
  </action>
  <verify>
    <automated>cargo test && wasm-pack build --target web && wasm-pack test --headless --chrome</automated>
  </verify>
  <acceptance_criteria>
    In physical mode after engine move, non-space keys do not resume; pressing Space resumes exactly once. Selecting No clock removes clock element from visible layout; timeout immediately sets game-over.
  </acceptance_criteria>
  <done>
    Physical wait state and clock rules operate exactly as specified during runtime.
  </done>
</task>

<task type="auto">
  <name>Implement end state and restart commands</name>
  <files>
    www/main.js
    src/lib.rs
  </files>
  <read_first>
    .planning/phases/01-core-chess-engine-ui/01-CONTEXT.md
    .planning/phases/01-core-chess-engine-ui/01-UI-SPEC.md
  </read_first>
  <action>
    Show inline game-over banner while keeping final board visible. Implement `resign` command with no confirmation. After game-over, prompt Enter/new to return to pre-game config.
  </action>
  <verify>
    <automated>node --check www/main.js && wasm-pack build --target web && wasm-pack test --headless --chrome</automated>
  </verify>
  <acceptance_criteria>
    Typing resign ends game immediately with inline banner and final board still visible; pressing Enter or typing new returns to pre-game config.
  </acceptance_criteria>
  <done>
    End-state banner, resign command, and Enter/new restart flow complete and keyboard-only.
  </done>
</task>

<task type="auto">
  <name>Add wasm behavior tests for runtime transitions</name>
  <files>
    tests/web.rs
  </files>
  <read_first>
    tests/web.rs
    .planning/phases/01-core-chess-engine-ui/01-CONTEXT.md
  </read_first>
  <action>
    Add web tests covering invalid PGN leaves board unchanged, physical-mode waits for Space, and game-over restart via Enter/new.
  </action>
  <verify>
    <automated>wasm-pack test --headless --chrome</automated>
  </verify>
  <acceptance_criteria>
    tests/web.rs includes named tests for invalid_pgn_no_state_change, physical_mode_space_resume, and game_over_restart_flow.
  </acceptance_criteria>
  <done>
    Behavior tests in tests/web.rs exist and execute in wasm browser test runner.
  </done>
</task>
</tasks>

<threat_model>
| Threat | Mitigation |
|---|---|
| Invalid runtime commands mutate state | allowlist command parser by game state |
| Pause/resume race | single authoritative game-state enum |
</threat_model>

<verification>
cargo test && wasm-pack build --target web && wasm-pack test --headless --chrome
</verification>
